# Hello World Project

This project contains the 0-preprocessor script that runs a C file through the preprocessor.
# Hello World Project

This project contains the 0-preprocessor script that runs a C file through the preprocessor.

